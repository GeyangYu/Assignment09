<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/brown</item>
        <item name="colorPrimaryDark">@color/darkbrown</item>
        <item name="colorAccent">@color/yellow</item>
        <item name="android:colorBackground">@color/lightbrown</item>
        <item name="android:buttonStyle">@style/NewButton1</item>
    </style>

    <style name="NewButton1" parent="android:style/Widget.Holo.Button">
        <item name="android:background">@drawable/button_new1</item>
    </style>
</resources>
